# Legacy cPanel redirect reference.
# Current Ubuntu production is hosted at /, not /atec/.
# If old links still use /Atec, redirect them to the root app.

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^Atec(/.*)?$ /$1 [R=301,L]
</IfModule>
